{
  "name": "tinhataiping",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "shared",
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "cd client && npx expo start",
    "build:shared": "npm run build --workspace=shared"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
